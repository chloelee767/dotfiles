# Dotfiles

## Setup
- soft link file/directory to relevant location using `ln -s`
- must be in the destination directory while creating the soft link

## Locations
- **.doom.d/** : HOME
- **.profile:** HOME
- **.zshrc** : HOME
- **init.vim** : HOME/.config/nvim
