#!/usr/bin/env bash

set -eou pipefail

# Check if an argument is provided
if [ $# -eq 0 ]; then
  count=1
else
  count=$1
fi

# Generate N UUIDs
for ((i=1; i<=count; i++)); do
  uuidgen | tr '[:upper:]' '[:lower:]'
done
