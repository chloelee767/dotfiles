[pull]
  rebase = true
[push]
  autoSetupRemote = true
[alias]
  uncommit = reset HEAD~
  unstage = reset HEAD --
  backup = "!f() { git branch "$(git rev-parse --abbrev-ref HEAD)"-backup; }; f"
  pull-other = "!f() { branch=$1; remote=${2:-origin}; git fetch \"$remote\" \"$branch:$branch\"; }; f"
